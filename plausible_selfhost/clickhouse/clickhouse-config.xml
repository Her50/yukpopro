<clickhouse>
  <!-- Dual-stack : :: accepte aussi IPv4 (IPV6_V6ONLY=0 par défaut Linux).
       Ne PAS spécifier 0.0.0.0 séparément sinon conflit "address in use". -->
  <listen_host>::</listen_host>
  <listen_try>1</listen_try>

  <!-- Logs : on garde l'essentiel pour debug Fly mais pas verbose -->
  <logger>
    <level>warning</level>
    <console>1</console>
  </logger>

  <!-- Paths persistés sur le volume Fly -->
  <path>/var/lib/clickhouse/</path>
  <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
  <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
  <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>

  <!-- Cf users.xml pour les limites mémoire/threads (settings user-level) -->
  <skip_check_for_incorrect_settings>1</skip_check_for_incorrect_settings>
</clickhouse>
