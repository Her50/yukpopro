# ─── YukpoAssurance — Dépendances complètes ────────────────────────────────────
# Version production — toutes fonctionnalités actives

# ─── IA & Modèles ──────────────────────────────────────────────────────────────
anthropic>=0.40.0
openai>=1.50.0
# YukpoTranslate Live — STT streaming (optionnel, fallback mode dégradé si absent)
deepgram-sdk>=3.7.0,<4.0.0

# ─── API & Web ─────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
sse-starlette>=2.1.0          # Server-Sent Events (streaming chat IA)
websockets>=13.0               # WebSocket collaboration

# ─── Base de données ───────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
asyncpg>=0.30.0
aiosqlite>=0.20.0              # SQLite async pour dev local
psycopg2-binary>=2.9.0
alembic>=1.13.0
pyodbc>=5.1.0                  # Connecteur ODBC SQL Server (ORASS, Mercure)
cx_Oracle>=8.3.0               # Connecteur Oracle (Saphir)

# ─── Cache ─────────────────────────────────────────────────────────────────────
redis>=5.0.0

# ─── OCR & Documents ──────────────────────────────────────────────────────────
Pillow>=10.0.0
pdf2image>=1.17.0
pytesseract>=0.3.10
opencv-python-headless>=4.9.0   # Prétraitement images OCR (deskew, denoise, binarisation)
pypdf2>=3.0.0
PyMuPDF>=1.24.0            # OCR PDF scanné — convertit les pages en images PNG pour Vision IA
openpyxl>=3.1.0
python-docx>=1.1.0
python-pptx>=1.0.0
fpdf2>=2.7.0

# ─── Traitement de données & ML ───────────────────────────────────────────────
pandas>=2.1.0
tabulate>=0.9.0                # Requis par pandas df.to_markdown()
numpy>=1.26.0
scikit-learn>=1.4.0            # ML tarification prédictive
scipy>=1.12.0                  # Calculs actuariels

# ─── Visualisation & Graphiques ───────────────────────────────────────────────
matplotlib>=3.8.0              # Graphiques intégrés dans PPT, rapports, Excel
seaborn>=0.13.0                # Heatmaps, distributions, graphiques stat avancés
folium>=0.16.0                 # Cartes géographiques interactives
xlrd>=2.0.0                    # Lecture fichiers .xls legacy ORASS/Mercure
kaleido>=0.2.1                 # Export SVG/PNG depuis plotly (optionnel, non bloquant)

# ─── Validation & Config ───────────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ─── Authentification & Sécurité ──────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0
python-dotenv>=1.0.0
slowapi>=0.1.9                 # Rate limiting
cryptography>=42.0.0
pyotp>=2.9.0                   # 2FA TOTP (Google Authenticator)
qrcode[pil]>=7.4.2             # QR Code pour setup 2FA

# ─── Monitoring & Observabilité ───────────────────────────────────────────────
prometheus-client>=0.20.0      # Métriques Prometheus
prometheus-fastapi-instrumentator>=7.0.0  # Instrumentation FastAPI auto
opentelemetry-sdk>=1.24.0      # Tracing distribué
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-exporter-otlp>=1.24.0

# ─── Utilitaires ───────────────────────────────────────────────────────────────
aiofiles>=23.2.0
tenacity>=8.2.0
structlog>=24.1.0
rich>=13.7.0
python-dateutil>=2.9.0

# ─── Email ─────────────────────────────────────────────────────────────────────
aiosmtplib>=3.0.0              # Envoi email async (notifications, rapports)
sendgrid>=6.11.0               # SendGrid API

# ─── PDF & Infographie Pro ──────────────────────────────────────────────────────
reportlab>=4.1.0               # Génération PDF avancé (rapports, certificats)
svgwrite>=1.4.3                # Export SVG vectoriel infographies
svglib==1.4.1                  # SVG → reportlab Drawing (embed vectoriel). Épinglé 1.4.1 : 1.5.0+ tire pycairo (libcairo2-dev requis au build)
pikepdf>=9.0.0                 # Manipulation PDF avancée (assemblage multi-page livrets, métadata PDF/X)
fonttools>=4.50.0              # Subsetting Google Fonts pour embed dans PDF
staticmap>=0.5.7               # Plans localisation (cérémonie deuil/mariage)
# Sprint 1.4 — WeasyPrint (HTML/CSS3 → PDF). Active sur Linux (Docker prod).
# Sur Windows local : import gracieux (try/except) — l'app fonctionne sans.
weasyprint>=62.0
# Sprint 2.2 — SAML SSO (Okta, Azure AD, Google Workspace, OneLogin)
# Requires libxmlsec1-dev (Linux). Sur Windows : import gracieux.
python3-saml>=1.16.0

# ─── WhatsApp / Notifications ──────────────────────────────────────────────────
twilio>=9.0.0

# ─── ML — Persistance modèles ─────────────────────────────────────────────────
joblib>=1.3.0                  # Sauvegarde/chargement modèles scikit-learn (ML tarification)

# ─── Recherche sémantique RAG CIMA ────────────────────────────────────────────
sentence-transformers>=3.0.0   # Embeddings denses pour RAG CIMA (paraphrase-multilingual-MiniLM-L12-v2)
torch>=2.0.0                   # Backend sentence-transformers (CPU-only suffisant)

# ─── Serveur WSGI/ASGI production ─────────────────────────────────────────────
gunicorn>=22.0.0               # Process manager multi-workers pour uvicorn en production

# ─── Tests (voir requirements-dev.txt pour l'environnement de développement) ──
# pytest, pytest-asyncio, pytest-cov → requirements-dev.txt
